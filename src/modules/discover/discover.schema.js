const Joi = require('joi');

// Schéma pour la récupération des langues (aucun paramètre requis)
exports.getLanguagesSchema = Joi.object({});

// Schéma pour la récupération des exercices (aucun paramètre requis)
exports.getExercisesSchema = Joi.object({});
